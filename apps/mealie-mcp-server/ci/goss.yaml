---
command:
  python-version:
    exec: "python --version"
    exit-status: 0
    stdout:
      - "Python 3.12"

  uv-installed:
    exec: "uv --version"
    exit-status: 0

file:
  /app/src/server.py:
    exists: true
    owner: mcp
