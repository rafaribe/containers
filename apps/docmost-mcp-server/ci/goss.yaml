---
command:
  node-version:
    exec: "node --version"
    exit-status: 0
    stdout:
      - "v22"

  docmost-mcp-installed:
    exec: "npm list -g docmost-mcp --depth=0"
    exit-status: 0
    stdout:
      - "docmost-mcp"
